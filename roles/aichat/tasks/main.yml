
- name: Fetch OpenAI key from 1Password
  shell: op read "op://private/OpenAI/API_KEY"
  register: openai_key
  changed_when: false

- name: Configure aichat env
  lineinfile:
    path: ~/.bashrc
    line: 'export OPENAI_API_KEY={{ openai_key.stdout }}'
    insertafter: EOF
